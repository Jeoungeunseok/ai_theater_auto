# 📡 본심 대변인 「팡이」 통합 실행 계획서 (v4)

> 자동화 아키텍처 반영판 — I2V 제작(v3) + **감독형 자동화(슬랙 휴먼인더루프) 되살림**
> 벤치마크: 정서불안 김햄찌 (이미지 생성 + I2V 방식)
> MacBook Air M3 · 16GB · 작성일: 2026-05-30 (v3 → v4 개정)

---

## 0. 무엇이 바뀌었나 (v3 → v4)

| 구분 | v3 | v4 |
| --- | --- | --- |
| 자동화 | "핵심은 수동, 주변부만 자동"으로 축소 | **감독형 자동화** — 생성·조립·전달은 자동, *판단만 슬랙으로* |
| 사람의 역할 | Kling UI에서 손작업 | **슬랙에서 승인/재생성/수정** (폰으로 게이트 통과) |
| 인프라 | 거창한 파이프라인 보류 | **FastAPI + RQ + Postgres + Slack** 되살림 (Kling 비동기 API에 딱 맞음) |
| 수정 | 명시 안 됨 | **수정 루프** — 피드백 → GPT 재작성 → 영향받는 컷만 재생성 |
| 핵심 원칙 | — | "자동이지만 게이트에서 사람이 거른다" |

> **왜 되살렸나:** Kling이 **비동기 REST API**(작업 제출 → 폴링)를 제공하므로, 파이프라인이 생성을 자동 호출하고 결과가 나오면 슬랙으로 알림 → 사람이 버튼으로 판단하는 구조가 완벽히 들어맞는다. v3에서 보류한 건 성급했음.

> **불변 철학:** 자동화 이전에 **수동 1편으로 품질부터 확정.** 사람은 "선별자·승인자"이지 "무한 생성기"가 아니다.

---

## 1. 하드웨어 — 가벼운 오케스트레이터 + 클라우드 생성

무거운 영상 생성은 **Kling 클라우드**가 대행. 로컬에 남는 건 *가벼운 오케스트레이터*(API 호출·작업상태 관리·슬랙 송수신)뿐이다.

| 항목 | 역할 |
| --- | --- |
| 오케스트레이터(FastAPI/RQ/Postgres) | API 호출·작업상태·슬랙 — **CPU·RAM 거의 안 먹음** |
| 영상 생성 | Kling 클라우드(외부) — 맥 자원 소모 X |
| 편집/검수 | 슬랙 + 필요 시 CapCut |

### 🎯 결론
- 개발·테스트는 맥북으로 충분
- ⚠️ **단, 비동기 콜백 때문에 오케스트레이터는 상시 가동이 유리.** 맥이 슬립이면 Kling 작업 완료 알림을 못 받아 파이프라인이 멈춤 → 초기엔 `caffeinate`로 버티되, **저렴한 상시 VPS(Oracle ARM 무료티어 등)로 오케스트레이터만 일찍 올리는 걸 권장.** (무거운 생성은 어차피 Kling이 하므로 작은 서버로 충분)

---

## 2. 제작 파이프라인 (I2V 코어)

### 2.1 핵심 흐름 (전부 API로 자동 호출)
```
① GPT      대본 + 컷별 이미지 프롬프트 + 컷별 움직임 프롬프트 (OpenAI API)
② 이미지   고정 팡이 레퍼런스 + 프롬프트로 컷당 후보 2~3장 (이미지 API)
③ I2V      선택된 이미지를 애니메이션 (Kling API — 비동기, 폴링)
④ 음성     대사 줄 TTS (감정별 rate/pitch는 pangi_persona.yaml)
⑤ 조립     클립·음성·자막·BGM·효과음 합성 (FFmpeg)
```

### 2.2 역할 분담
- **GPT = 글·지시** / **이미지 API = 그림** / **Kling = 움직임(I2V)** / **TTS = 음성** / **FFmpeg = 조립**

### 2.3 ⭐ 일관성 — 생사 포인트
- 모든 컷 이미지 생성 시 **확정된 "팡이 마스터 레퍼런스"를 반드시 주입**
- Kling **Elements/Subject(레퍼런스 고정)** 기능으로 컷마다 동일 팡이 유지
- 14감정은 표정·안테나 색만 프롬프트로 지정, *얼굴 구조·비율은 레퍼런스로 고정*

### 2.4 이미지를 어디서 — Kling 권장
- 기본값: **Kling 안에서 이미지+I2V** (한 툴, 일관성 기능 통합, 내보내기 마찰 X)
- ⚠️ 같은 컷 2~3개를 GPT 이미지 vs Kling 이미지로 비교 후 확정

### 2.5 Kling API 운영 주의
- **비동기 작업 모델**: 생성 요청 → 작업 ID 폴링(완료까지 수 분). RQ 큐로 처리.
- **생성 링크 24시간 유효** → 완료 즉시 로컬/스토리지에 저장(안 하면 만료).
- 모든 모드(T2V·I2V·편집) API 제공, 표준 REST.

### 2.6 ⭐ 조립·편집 자동화 규칙 (⑤ 단계 상세)
> 컷을 "그냥 붙이기"는 자동 가능. 하지만 완주율을 좌우하는 *편집 감각*을 규칙으로 코드화해야 김햄찌급 퀄이 나온다. 아래 규칙을 FFmpeg 조립에 박아넣고, 못 잡는 부분만 슬랙 프리뷰에서 사람이 미세 수정.

**(1) 호흡 / 컷 길이**
- 후킹: 첫 1초 안에 임팩트(정지프레임·줌인 허용)
- 꿀팁 컷: 3~5초, **말 끝나면 0.3~0.5초 트림**(데드에어 제거)
- 비트 전환은 **하드컷**(빠른 템포) — 늘어지는 디졸브 금지
- 전체 30초 타이트 유지

**(2) 효과음(SFX) 맵 — 비트 타입별 자동 삽입**
- 후킹: 시선 끄는 "확!" / 본심 수신: 시그니처 신호음 "삐빅 📶"
- 꿀팁 등장: "딩!"·"두구두구" / 펀치라인·반전: 임팩트 "쾅"
- 젤리 등장(허당 개그): 신호 끊김음 "띠로링~"(멍함 😵)
- 표준 SFX 라이브러리를 **비트 태그에 매핑**해 자동 배치

**(3) 자막 스타일**
- 카테고리별 색은 `work.yaml` 등 `subtitle_style` 그대로 사용
- **핵심 단어 강조**(크게·색 변경), TTS 타임스탬프로 말에 싱크
- 하단 중앙 1~2줄, 가독성 우선(화면 가림 최소)

**(4) BGM**
- 카테고리별 `bgm` 경로 사용, 볼륨은 음성보다 낮게(덕킹), 후킹·펀치에 살짝 강조

**(5) 후킹·루프**
- 첫 프레임에 텍스트 후킹 오버레이(썸네일 겸용)
- 엔딩을 첫 컷과 이어지게 → **루프 재시청 유도**

> **확정 방법:** 위 규칙의 *구체값*(정확한 컷 길이, SFX 선택, 강조 규칙)은 **Phase 1 수동 1편**에서 손으로 만들며 발견 → 자동 조립 코드에 고정. 이후 편들은 자동 조립 + 슬랙에서 "3번 컷 0.5초 줄여 / 여기 효과음 교체" 정도만 수정.

---

## 3. ⭐ 감독형 자동화 아키텍처 (v4 핵심)

### 3.1 개념
파이프라인이 단계마다 **자동 생성**하고, 사람의 판단이 필요한 지점에서만 **슬랙으로 결과를 던져** 승인/재생성/수정을 받는다. 무인 자동도, 전수 수동도 아닌 **그 중간(supervised)**.

### 3.2 인프라 (v1/v2 구조 부활)
- **FastAPI** — 작업 수신 + 슬랙 액션 수신 엔드포인트
- **RQ(Redis)** — 비동기 큐(생성/조립/업로드 분리), Kling 폴링 처리
- **PostgreSQL** — 작업·**버전 상태** 관리 (어느 컷이 승인됐고 어느 게 v2인지)
- **Slack Interactive** — `[승인] [재생성] [수정하기]` 버튼 + 피드백 입력 모달
  - `X-Slack-Signature` HMAC 검증 / 로컬 개발은 ngrok·Cloudflare Tunnel

### 3.3 단계별 슬랙 게이트 (비용 순서가 핵심)
> **싼 단계 게이트를 먼저, 비싼 I2V는 사람이 고른 것만.**
1. **대본·프롬프트(GPT, 자동)** → 슬랙 "대본 확인" → 승인/수정
2. **이미지 후보(자동, 컷당 2~3장)** → 슬랙에 후보 나열 → 컷마다 베스트 선택/재생성 *(싸니까 여기서 먼저 거름)*
3. **선택 이미지만 I2V(Kling, 자동)** → 슬랙 클립 미리보기 → 승인/반려/재생성 *(비싼 단계 — 고른 것만)*
4. **음성·자막·조립(자동)** → 슬랙 최종 프리뷰 → 최종 승인
5. **승인 시 → 업로드 큐(자동)**

### 3.4 수정 루프 — "어디서부터 다시 돌릴지"를 나눔
"수정하기"로 들어온 피드백은 레벨에 따라 *영향받는 부분만* 재생성한다:
- **대본 레벨**("톤 더 자극적으로") → GPT 대본 재작성 → 하위 전부 재생성
- **컷 레벨**("3번 컷 표정 심술로") → 그 컷의 이미지 프롬프트·이미지·I2V만 재생성
- **움직임 레벨**("이 클립 어색해") → 같은 이미지로 I2V만 재생성

루프: `결과 → 슬랙[승인/재생성/수정] → (수정 시) 피드백 입력 → 원본+피드백을 GPT에 → 영향 컷만 재생성 → 새 버전 슬랙 → 승인까지 반복`

**규칙:** 승인된 컷은 **잠금**(재생성 안 함). 비싼 I2V는 **고친 그 컷만** 다시. 대본·이미지는 싸니 넉넉히 재생성 OK.

### 3.5 비용 가드
- **에피소드당 재생성 횟수 상한** 설정 → 초과 시 슬랙 경고("이번 편 리롤 한도 도달")
- `daily_cost` 워커: 일일 누적 비용 임계치 초과 시 큐 일시정지

---

## 4. 로드맵

### Phase 0 — 페르소나·레퍼런스·계정 (1주)
- [ ] 팡이 페르소나 확정 + `pangi_persona.yaml` 보강(§6)
- [ ] **팡이 마스터 레퍼런스 세트 확정** (일관성 기준점)
- [ ] 계정/키: OpenAI, **Kling API**, TTS, Slack, YouTube

### Phase 1 — 수동 1편 끝까지 (2주) ★핵심 의식
- [ ] GPT 대본 + 컷별 프롬프트 ("상사 몰래 쉬는 법")
- [ ] 이미지 8~10컷(레퍼런스 주입, 리롤 선별) → Kling I2V → TTS → 편집
- [ ] **완성·업로드**로 톤·일관성·완주율 검증
- [ ] **반복 가능한 프롬프트 템플릿**(컷타입·감정별) 추출 ← 파이프라인 입력값이 됨

### Phase 2 — 비동기 파이프라인 (2주)
- [ ] FastAPI `/jobs` + RQ 큐(생성/조립/업로드 분리)
- [ ] Kling API 연동(비동기 작업·폴링·24h 링크 즉시 저장)
- [ ] Postgres 스키마(작업·컷·버전·투표·제보):
  ```
  jobs(id, status, current_step, retry_count, ...)
  cuts(job_id, cut_no, image_ver, video_ver, status, locked)
  episodes(id, category, episode_no, youtube_id, ...)
  topics / submissions / votes   -- 소재엔진·투표용
  ```
- [ ] 재시도 지수 백오프 + 멱등키(중복 업로드 차단)

### Phase 3 — 슬랙 게이트 + 수정 루프 (2주)
- [ ] Slack Interactive(`승인/재생성/수정` + 피드백 모달) + HMAC 검증
- [ ] 단계별 게이트(§3.3) 구현 — 싼 단계 먼저
- [ ] 수정 루프(§3.4) — 레벨별 타깃 재생성 + 승인 컷 잠금
- [ ] 비용 가드(§3.5) — 재생성 상한·일일 비용 차단
- [ ] **조립·편집 규칙(§2.6) 코드화** — 컷 길이·SFX 맵·자막 강조를 FFmpeg 조립에 반영(Phase 1에서 확정한 값)

### Phase 4 — 소재 엔진 + 업로드 자동 (2주)
- [ ] 4단 소재 엔진(§5) 연동 — 제보·트렌드·아키타입·AI 오리지널
- [ ] YouTube 자동 업로드(Resumable, OAuth, 메타데이터 공식)
- [ ] 댓글 "다음 본심 주제" 투표 수집·집계 → 다음 화 시드

### Phase 5 — 확장 (지속)
- [ ] 본심 카테고리 확장(직장·욕망·부부·일상, `/prompts/*.yaml`)
- [ ] 굿즈 IP(팡이 이모티콘→실물, 자체 IP라 저작권 충돌 0)
- [ ] 다채널 시 오케스트레이터 VPS 상시 가동

---

## 5. 소재 엔진 (떨어지지 않는 4단 우물)
1. **시청자 제보**(장기 메인): "당신의 본심, 팡이가 대신" CTA + 폼, 동의·익명화
2. **트렌드 마이닝**: 커뮤니티·유사채널 댓글에서 *유형*만 추출(원문 X) + 데이터랩 검증
3. **아키타입 뱅크**: "상황 × 능청 해법" 조합 → 무한 생성
4. **AI 오리지널**: 위 입력으로 GPT가 새 사연 (저작권·양산정책 안전)

> 배합: 초기 2·3·4 메인 → 성장하며 1번(제보)이 메인. 전부 오리지널 표현.

---

## 6. 페르소나 파일 보강 필요 (`pangi_persona.yaml`)
- [ ] **젤리 약점**: `favorite_snack: 젤리` + `weakness`(젤리 보면 신호 끊김 😵→재부팅, 허당미·러닝개그)
- [ ] **홈 감정 명시**: 기본 심술, 마무리 자신감·뿌듯, 분노 최소
- [ ] **감정 14종 통일**: "눈물"(15번째) 정리 여부 결정
- [ ] **비트 4개 확정**: 후킹·본심수신·꿀팁3단·마무리 (30초 적합)

---

## 7. 비용 가드레일 (정확한 단가는 현재 Kling 요금제 확인)

| 항목 | 비고 |
| --- | --- |
| GPT(대본·프롬프트) | 저렴, 월 ~$15 |
| 이미지 생성 | 컷당 소액 + 후보 2~3장 배수 |
| **Kling I2V** | **최대 비용** — 컷당 × 8~10컷 × 리롤 × 편수 |
| TTS | Edge-TTS 무료 / 유료 옵션 |
| VPS(오케스트레이터) | 소형 — 무료티어 가능 |

> **핵심:** I2V는 *생성·리롤이 곧 돈*. **슬랙 게이트로 리롤을 거르고**(특히 싼 이미지 단계에서 먼저), **에피소드당 재생성 상한**으로 폭주 방지. Kling은 **월 구독(크레딧 묶음)** 티어가 종량제보다 유리.
> ⚠️ Kling 요금제·API 단가는 자주 바뀌므로 **계약 전 최신 요금 직접 확인.**

---

## 📌 즉시 다음 액션 (이번 주)
1. **팡이 마스터 레퍼런스 세트 확정** (일관성 기준점 — 최우선)
2. `pangi_persona.yaml` 보강(젤리·홈감정·14통일·4비트)
3. Kling 계정·**API 접근**·요금제 확인 + GPT 이미지 vs Kling 이미지 일관성 비교 테스트
4. **Ep.01 수동 1편 만들기**("상사 몰래 쉬는 법") — 전 과정 손으로 관통 (→ 파이프라인 템플릿 추출)
